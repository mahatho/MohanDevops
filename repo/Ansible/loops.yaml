- name: Loop example of installations
  hosts: web
  become: yes
  tasks:
  - name: Installing packages on loop
    ansible.builtin.dnf:
     name: "{{ item }}"
     state: latest
    loop:
    - nginx
    - ansible
    - mysql